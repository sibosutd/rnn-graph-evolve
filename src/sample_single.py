"""
Sample single graph from the datasets.

Use networkx library.
"""

import sys
import networkx as nx
import numpy as np

import glob

glob.set_dir()
TYPE = 'RANDOM'
INDEX = 0

# sequences = np.load(glob.DATASET_PATH+TYPE+'.npy')
